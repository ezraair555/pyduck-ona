# `span_vs_depth`

**Module:** `pyduck_ona.viz.span_control`

## Signature

```python
span_vs_depth(df'pd.DataFrame', id_col'str'='manager_id', span_col'str'='direct_reports', depth_col'str'='levels_below', team_col'str'='total_reports', label_col'str | None'=None, metadata'pd.DataFrame | None'=None, name_col'str'='name', title'str | None'=None, figsize'tuple[float, float]'=(10.0, 7.5))
```

## Description

Quadrant bubble chart of span × depth with team size as bubble area

## Returns

-------
matplotlib.figure.Figure

## Example

```python
import pyduck_ona as pona
# TODO: add a runnable example
```

---

[Back to API catalog](../README.md#api-catalog)
